# Keycloak — the identity provider for all three consuming companies.
#
# Three realms, three issuers. Each company authenticates against its OWN realm
# with its own signing keys, its own clients, and its own users:
#
#   http://keycloak.mcp-federation.svc.cluster.local:8180/realms/acme
#   http://keycloak.mcp-federation.svc.cluster.local:8180/realms/globex
#   http://keycloak.mcp-federation.svc.cluster.local:8180/realms/initech
#
# To agentgateway these are three independent OIDC providers — it validates each
# against a different JWKS endpoint. Running them as realms inside one Keycloak
# keeps the demo to a single pod; swapping any realm for a genuinely separate
# Keycloak (or Okta, Entra, Auth0 — see the `provider` field agentgateway
# supports) is a change of issuer URL and nothing else.
#
# KC_HOSTNAME is pinned to the in-cluster service URL on purpose. Keycloak
# derives the `iss` claim from the hostname it is serving under, so without this
# a token minted through `kubectl port-forward` would carry iss=http://localhost:8080
# and the gateway — which validates against the in-cluster issuer — would reject
# it. Pinning the hostname means a token works identically whether it was
# requested from inside the cluster or from your laptop.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: keycloak
  namespace: mcp-federation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keycloak
  template:
    metadata:
      labels:
        app: keycloak
    spec:
      containers:
      - name: keycloak
        image: quay.io/keycloak/keycloak:26.0
        args: ["start-dev", "--import-realm"]
        env:
        - name: KC_BOOTSTRAP_ADMIN_USERNAME
          value: admin
        - name: KC_BOOTSTRAP_ADMIN_PASSWORD
          value: admin
        - name: KC_HOSTNAME
          value: http://keycloak.mcp-federation.svc.cluster.local:8180
        - name: KC_HOSTNAME_STRICT
          value: "false"
        - name: KC_HTTP_ENABLED
          value: "true"
        # Listen on 8180 (not Keycloak's default 8080). The port is part of the
        # issuer URL, and browser SSO for the UI requires the SAME port to be
        # forwardable on the presenter's machine — where 8080 is routinely taken
        # by Docker. 8180 everywhere: listen port, Service port, issuer, local
        # forward. See port-forward.sh and the UI section of the README.
        - name: KC_HTTP_PORT
          value: "8180"
        ports:
        - containerPort: 8180
          name: http
        readinessProbe:
          httpGet:
            path: /realms/acme
            port: 8180
          initialDelaySeconds: 15
          periodSeconds: 5
          failureThreshold: 40
        resources:
          requests: { cpu: 100m, memory: 512Mi }
        volumeMounts:
        - name: realms
          mountPath: /opt/keycloak/data/import
      volumes:
      - name: realms
        configMap:
          name: keycloak-realms
---
apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: mcp-federation
spec:
  selector:
    app: keycloak
  ports:
  - name: http
    port: 8180
    targetPort: 8180
